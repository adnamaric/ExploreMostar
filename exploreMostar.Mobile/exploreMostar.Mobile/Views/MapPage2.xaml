<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:exploreMostar.Mobile.ViewModels"
             x:Class="exploreMostar.Mobile.Views.MapPage2">
    <ContentPage.BindingContext>
        <viewmodels:TempModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>


        <ListView x:Name="MyListView"
            ItemsSource="{Binding Restorani}"
          
            CachingStrategy="RecycleElement">
            <!--Built in Cells-->


            <!--Custom View Cells-->

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Naziv}" 
                   Style="{DynamicResource ListItemTextStyle}" />
                            <Label Text="{Binding Lokacija}" 
                   Style="{DynamicResource ListItemDetailTextStyle}"/>
                         
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        




    </ContentPage.Content>
</ContentPage>